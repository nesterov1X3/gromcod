export const cloneArr = arr => {
    if(!Array.isArray(numbers)){
        return null;
    }

    const ArrayNumberClone = arr.slice()
    return ArrayNumberClone
}
const numbers = [4, 6, 2, 5]
// console.log(cloneArr(numbers))